I need to push the local `publish-hard-cap` branch (which contains 51 commits ahead of the current GitHub main) to the `github` remote's `main` branch, since Hostinger deploys from GitHub main and it's currently very stale (last deploy Aug 29, before this entire session's fixes).

A previous force-with-lease push attempt was rejected with "stale info" — meaning the local knowledge of github/main was outdated at the time of the push.

Please do the following, in order, and report the output of each step:

1. Run `git fetch github` to get the current state of the remote.
2. Run `git log github/main -5 --oneline` to show the current tip of remote main.
3. Compare this against the local `publish-hard-cap` branch history — confirm whether the commits on github/main beyond the common ancestor are genuinely new/different content, or just the same historical commits with different hashes (as we saw earlier: commits like "Add reliable two-call analysis pipeline", "Reject unsupported analysis topics", "Align evidence validation with prompt" appear on both sides with different hashes, suggesting a prior rebase rather than real divergent work).
4. If the remote's extra commits are confirmed to be content-equivalent to commits already present in `publish-hard-cap`'s history (not genuinely new/different work), proceed to push: `git push github publish-hard-cap:main --force-with-lease`.
5. If the remote has commits that are NOT already represented in `publish-hard-cap`'s history (genuinely new/unique work), STOP and report back what those commits are and what they changed — do not force-push over unique remote work without explicit confirmation.
6. After a successful push, run `git log github/main -3 --oneline` again to confirm main now points to our latest commit.

Note: there is an existing `gitsafe-backup` remote pointing to the same commit as our current HEAD, which serves as a safety net if anything needs to be recovered.

Report each step's output clearly before proceeding to the next.