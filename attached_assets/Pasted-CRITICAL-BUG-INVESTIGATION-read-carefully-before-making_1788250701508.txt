CRITICAL BUG INVESTIGATION — read carefully before making any change.

Symptom (now confirmed across MULTIPLE different subjects — Contract/LLP business-law papers AND Organizational Behaviour papers): In recent analyses, several topics in the final result have "0 kya_padhna_hai bullets (needs 4-6)" as a quality issue, meaning these topics reached the final output with essentially EMPTY study content, despite:
- The system prompt still requiring 4-6 newline-separated "- " bullets in study_note.kya_padhna_hai for every topic (confirmed unchanged in the prompt text).
- validateAiAnalysisResult supposedly filtering out topics without complete study notes via hasNonEmptyStudyNotes (or equivalent).

Additionally, in the same analyses, dozens of topics that the AI itself listed as "distinctive_topics" in its own paper_summaries are missing from the final topics array entirely — meaning either the initial generation is dropping many topics it identified, or something in the repair/recovery pipeline is failing to add them despite our existing getUncoveredDistinctiveTopicIssues check flagging them correctly (it IS flagging them — that's how these appear in quality_issues — but the repair call is not fixing most of them).

This is a severe, reproducible regression from the ~40-46 rich, complete topics these same benchmark files produced in earlier testing sessions, now consistently producing 14-18 topics with many having empty study notes.

Your task:
1. Find the exact function that validates study_note completeness before a topic is allowed into the final result (likely hasNonEmptyStudyNotes in artifacts/api-server/src/lib/openai.ts). Show its current implementation. Confirm: does it check ONLY for non-empty string length (e.g., `.trim().length > 0`), or does it also validate bullet-count/format (matching /^\s*-\s+/gm counting 4-6 matches, same regex used in getTopicQualityIssues' invalidStudyNotes check)? If it only checks non-empty length, a topic with a single short sentence (not 4-6 bullets) passes this gate and reaches the final result — THIS IS LIKELY THE ROOT CAUSE. The completeness gate that determines final inclusion must be at least as strict as the quality-issue check that reports the problem afterward, or topics with insufficient bullets will always leak through into "accepted" results.
2. Inspect why so many AI-self-identified distinctive_topics are not making it into the final topics array even after the targeted repair call. Check makeTargetedRepairRequest's prompt and the repair-issue list construction — is the repair call being given ALL the missing-topic issues, or is something truncating/limiting how many missing-topic issues get surfaced to it in one pass (recall the hard-cap is only 2 total AI calls, so if 30+ distinctive topics are missing, the single repair call cannot realistically add all of them in one shot — this may be a fundamental limitation of the 2-call hard cap combined with how much the initial call is now under-generating, rather than a new bug per se). Report whether the initial call's topic count itself has become lower than before, and if so, why (check if anything in the surrounding code changed how paperContent, subjectSpecificGuidance, or the user prompt gets constructed — even subtle truncation or a construction bug would explain fewer initial topics).
3. Determine the root cause and report it clearly BEFORE proposing a fix: is it (a) hasNonEmptyStudyNotes being too lenient (letting sparse content through), (b) the initial AI call itself now generating measurably fewer/sparser topics than the original prompt used to produce (something changed in how the request is built), (c) the repair call being overwhelmed with too many missing-topic issues to fix in one bounded call, or (d) something else entirely.
4. Propose and implement the minimal, targeted fix:
   - If (a): tighten hasNonEmptyStudyNotes to also require 4-6 properly formatted bullets, matching the same standard used elsewhere, so incomplete topics are correctly excluded rather than leaking through as "degraded but accepted."
   - If (b): identify and fix whatever is causing the initial call to under-generate compared to before.
   - If (c): this may not be fixable within the existing 2-call hard cap and should be reported as a structural limitation rather than "fixed" — do not attempt a workaround that violates the 2-call cap or the "fewer real topics are better than fabricated padding" principle.

Do NOT change the system prompt content, model selection, the catastrophic-quality-floor safeguard, or the credits/billing logic. This must be a precise, root-cause-driven fix, not a broad rewrite.

After identifying and fixing the root cause, run the full test suite, and add a regression test verifying that a topic with fewer than 4 kya_padhna_hai bullets can never appear in the final returned topics array — it must be excluded or fully repaired, never left in a "degraded but accepted" state with insufficient content.

Report your root-cause finding clearly and explicitly (state which of a/b/c/d it is) before describing your fix.