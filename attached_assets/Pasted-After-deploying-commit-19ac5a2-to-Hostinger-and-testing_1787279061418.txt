After deploying commit 19ac5a2 to Hostinger and testing again with the same 4 PDFs, I'm now getting: "We could not read enough text from the uploaded papers. Please use clear, unlocked PDFs or images and try again. Your credit has been refunded." — this is the new safe-failure message, which means OCR still isn't extracting text successfully in the Hostinger production environment, even after bundling the Tesseract language model.

Important additional clue: this same OCR extraction worked perfectly fine when testing on Replit's own Preview environment (before Hostinger deployment) — the exact same PDFs were read successfully there. This confirms the extraction code/logic itself is correct; the issue is specifically something different between Replit's runtime environment and Hostinger's production environment (most likely the bundled Tesseract model file not being present, not built, or not accessible at the expected path specifically on Hostinger).

Please check directly (don't re-investigate from scratch, just verify these specific things since you already found the root cause once):

1. Confirm the bundled Tesseract language model file actually exists at the expected path in the Hostinger production deployment (not just in the local/dev build) — check if the build/deploy process is correctly including it, since Hostinger's build output structure might differ from what you tested locally.

2. Check the latest failed analysis record's logs/error details (the specific per-file extraction logs you added earlier) to see exactly what error Tesseract threw this time on Hostinger — is it still a "model not found/download failed" error, or something new (e.g., file permission issue, path mismatch)?

3. If the bundled model path is wrong for Hostinger's specific directory structure, fix the path resolution to work correctly in that environment specifically.

Test by having me reproduce it again if needed, but first check the actual production logs for the exact current error before making changes.